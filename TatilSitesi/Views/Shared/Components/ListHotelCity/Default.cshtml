﻿@using TatilSitesi.Models
@model IEnumerable<City>

    <select style="width:50%;padding: 4px 10px;outline:none" name="CityId">
        @foreach (var item in Model)
        {
            <option value="@item.CityId">@item.CityName</option>
        }
    </select>
