﻿@using TatilSitesi.Models
@model IEnumerable<User>

<select style="width:50%;padding: 4px 10px;outline:none;" name="UserId">
    @foreach (var item in Model)
    {
        <option value="@item.UserId">@item.UserName</option>
    }
</select>