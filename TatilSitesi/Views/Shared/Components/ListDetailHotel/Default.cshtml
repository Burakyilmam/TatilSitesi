﻿@using TatilSitesi.Models
@model IEnumerable<Hotel>

<select style="width:50%;padding: 4px 10px;outline:none;" name="HotelId">
    @foreach (var item in Model)
    {
        <option value="@item.HotelId">@item.HotelName</option>
    }
</select>