@model TatilSitesi.Models.HotelDetail
@{
    ViewData["Title"] = "Otel Bilgileri";
    Layout = "~/Views/Shared/LayoutAdmin.cshtml";
}
<style>
    [disabled] {
        opacity: 1;
        color: black;
        border: 1px solid black;
    }
</style>
<div class="container-fluid">
    <h2 class="text-center mt-3 mb-3">Otel Bilgileri</h2>
    <center>
        @using (Html.BeginForm("HotelDetailUpdate", "HotelDetail", FormMethod.Post))
        {
            @Html.TextBoxFor(x=>x.HotelId,new {@style="display:none;"})

            <h2 class="text-center col-12">Otel</h2>
            @Html.DropDownListFor(model => model.HotelId, ((IEnumerable<SelectListItem>)ViewBag.Hotels).Select(c => new SelectListItem
        {
        Value = c.Value,
        Text = $"{c.Value} - {c.Text}"
        }),new {style="border-radius: 5px;padding: 4px 10px;width:100%",@readonly = "readonly",@disabled = "disabled" })


            <h2 class="text-center col-12">Animasyon</h2>
            @if (Model.Animation == true)
            {
                @Html.TextBox("Animation", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Animation", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Oyun Odası</h2>
            @if (Model.GameRoom == true)
            {
                @Html.TextBox("GameRoom", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("GameRoom", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Disko</h2>
            @if (Model.Disco == true)
            {
                @Html.TextBox("Disco", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Disco", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Klima</h2>
            @if (Model.AirConditioning == true)
            {
                @Html.TextBox("AirConditioning", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("AirConditioning", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">AquaPark</h2>
            @if (Model.AquaPark == true)
            {
                @Html.TextBox("AquaPark", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("AquaPark", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Balkon</h2>
            @if (Model.Balcony == true)
            {
                @Html.TextBox("Balcony", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Balcony", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Hamam</h2>
            @if (Model.Bath == true)
            {
                @Html.TextBox("Bath", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Bath", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Plaj</h2>
            @if (Model.Beach == true)
            {
                @Html.TextBox("Beach", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Beach", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Kapalı Restorant</h2>
            @if (Model.IndoorRestaurant == true)
            {
                @Html.TextBox("IndoorRestaurant", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("IndoorRestaurant", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Açık Restorant</h2>
            @if (Model.OutdoorRestaurant == true)
            {
                @Html.TextBox("OutdoorRestaurant", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("OutdoorRestaurant", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            <h2 class="text-center col-12">Kapalı Havuz</h2>
            @if (Model.OutdoorPool == true)
            {
                @Html.TextBox("OutdoorPool", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("OutdoorPool", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Açık Havuz</h2>
            @if (Model.IndoorPool == true)
            {
                @Html.TextBox("IndoorPool", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("IndoorPool", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Internet</h2>
            @if (Model.Internet == true)
            {
                @Html.TextBox("Internet", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Internet", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Masaj</h2>
            @if (Model.Massage == true)
            {
                @Html.TextBox("Massage", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Massage", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Minibar</h2>
            @if (Model.Minibar == true)
            {
                @Html.TextBox("Minibar", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Minibar", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Oda Servisi</h2>
            @if (Model.RoomService == true)
            {
                @Html.TextBox("RoomService", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("RoomService", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Sauna</h2>
            @if (Model.Sauna == true)
            {
                @Html.TextBox("Sauna", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Sauna", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Duş</h2>
            @if (Model.Shower == true)
            {
                @Html.TextBox("Shower", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Shower", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Şezlong</h2>
            @if (Model.Sunbed == true)
            {
                @Html.TextBox("Sunbed", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Sunbed", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Televizyon</h2>
            @if (Model.TV == true)
            {
                @Html.TextBox("TV", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("TV", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Masa Tenisi</h2>
            @if (Model.TableTennis == true)
            {
                @Html.TextBox("TableTennis", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("TableTennis", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Voleybol</h2>
            @if (Model.Volleyball == true)
            {
                @Html.TextBox("Volleyball", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Volleyball", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Lobi Barı</h2>
            @if (Model.LobyBar == true)
            {
                @Html.TextBox("LobyBar", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("LobyBar", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Basketbol</h2>
            @if (Model.Basketball == true)
            {
                @Html.TextBox("Basketball", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Basketball", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Otopark</h2>
            @if (Model.CarPark == true)
            {
                @Html.TextBox("CarPark", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("CarPark", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Çocuk Parkı</h2>
            @if (Model.ChildPark == true)
            {
                @Html.TextBox("ChildPark", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("ChildPark", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

            <h2 class="text-center col-12">Futbol</h2>
            @if (Model.Football == true)
            {
                @Html.TextBox("Football", "Aktif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }
            else
            {
                @Html.TextBox("Football", "Pasif", new { style = "border-radius: 5px; padding: 4px 10px; width: 100%", @readonly = "readonly" })
            }

        }
    </center>
</div>